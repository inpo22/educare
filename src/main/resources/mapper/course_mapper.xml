<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC   "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.edu.care.dao.CourseDAO">
	<select id="courseList" resultType="course">
		SELECT c.*
				, u.name 
		FROM course c
		JOIN user u on u.user_code  = c.user_code
	</select>
	
	
	<select id="reservationTime" resultType="course">
		select HOUR(end_time) as time,course_space from course_rez where YEAR(start_time) = YEAR(#{param1}) and  MONTH(start_time) = MONTH(#{param1}) and  DAY(start_time) = DAY(#{param1}) and course_space = #{param2}
	</select>
</mapper>